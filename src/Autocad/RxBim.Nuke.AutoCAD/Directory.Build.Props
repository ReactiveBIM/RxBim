<Project>
    
    <PropertyGroup>
        <NotRefenceApi>true</NotRefenceApi>
        <Version>2.1.0-dev003</Version>
    </PropertyGroup>

    <Import Project="$([MSBuild]::GetPathOfFileAbove('Solution.Build.props', '$(MSBuildThisFileDirectory)../'))" />
    <Import Project="$([MSBuild]::GetPathOfFileAbove('Names.Build.props', '$(MSBuildThisFileDirectory)../'))" />
    
</Project>