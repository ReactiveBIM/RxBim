<Project>
    <PropertyGroup>
        <ApiVersion>2.0.2</ApiVersion>
    </PropertyGroup>

    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))"/>
    <Import Project=".\RxBim.Build.Props" Condition="Exists('.\RxBim.Build.Props')"/>

</Project>